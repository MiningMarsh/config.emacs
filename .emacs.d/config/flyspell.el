;;; flyspell --- Enable spell checking for code and text documents.
;;; Commentary:
;;; Code:
;;(packages/requires (flyspell)
;;	   (add-hooks (prog-mode-hook)
;;		      (flyspell-prog-mode +1)))
;;; flyspell.el ends here
