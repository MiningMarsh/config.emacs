;;; ido-grid-mode-start-collapsed --- Make grid-mode a bit less intrusive.
;;; Commentary:
;;; Code:
(require 'rc)

(requiring (ido-grid-mode)
	   (setq ido-grid-mode-start-collapsed t))
;;; ido-grid-mode-start-collapsed.el ends here
