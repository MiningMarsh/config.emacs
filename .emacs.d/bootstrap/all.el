;;; all --- Library that is included for every config file.
;;; Commentary:
;;; This file is NOT loaded for libraries in bootstrap by default, they must still
;;; include packages and rc manually if they need it.
;;; Code:
(require 'rc)
(require 'packages)

(provide 'all)
;;; all.el ends here
